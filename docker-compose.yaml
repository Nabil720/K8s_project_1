
services:
  app:
    build: .
    container_name: portfolio-app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - .:/usr/src/app
    command: ["npm","start"]
